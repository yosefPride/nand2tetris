1)
    Instead of passing the number of local vars to a function, I might need to pass the number of args.
2)
   The way I'm accessing the "this" segment might be wrong.
3)
    Instead of calling the name of the variable that holds the address of an object dot the name of the method,
    I need to find a way to call the name of the class dot the name of the method.
4)
    To access a field, point to the "this" segment using push pointer 1 and then access the individual fields by adding the indexes.
5)
    When checking Jack code alongside VM code, make sure the variables are always in the right scope.
    currently nothing is being done to check in both scopes. !!!(this.varName tells me that it's a field. but what about static varibles.)!!!
6)
    when calling a function, i don't think i should push argument 0, but rather this 0. but actual
7)
    in compile term, when a method is called method() instead of var.method(), i need to push "this".